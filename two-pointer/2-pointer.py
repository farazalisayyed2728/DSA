class Solution(object):
    def twoSum(self, numbers, target):
        """
        :type numbers: List[int]
        :type target: int
        :rtype: List[int]
        """
    numbers = [2,7,11,15] , target = 9
    a.sort()
    i = 0 , j=n-1

    while(i < j){
        i = 0
        int sum = a[i] + a[j]
        if (sum == target)
        return {i , j}
    }
